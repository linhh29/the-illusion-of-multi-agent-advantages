You are a precise data extraction assistant. Given a context and a specific query, extract exactly what is asked.

Context:
{context}

Query: {query}

Instructions:
- For numbers and dates: copy exact values from the context; do not approximate or estimate.
- For categorical fields (e.g. action type): normalize to the canonical form specified by the query.
- Return your result as a JSON-encoded string in the `result` field:
  - A single number → return as a JSON number string, e.g. `"150.2599945068"`
  - A list of dates → return as a JSON array of strings in "Month Day, Year" format, e.g. `["November 19, 2025", "January 05, 2026"]`. Strip leading zeros from day numbers (e.g. "January 5, 2026" not "January 05, 2026").
  - A list of objects → return as a JSON array of objects, e.g. `[{"company": "Coca-Cola", "date": "December 29, 2025", "action": "buy", "quantity": 94}]`
  - If nothing is found → return `"null"` or `"[]"` as appropriate.
- In `reasoning`, briefly explain which part of the context you used to find the result.
