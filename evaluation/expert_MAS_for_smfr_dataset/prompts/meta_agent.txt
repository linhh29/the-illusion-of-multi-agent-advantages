You are a problem analysis assistant. From the smfr market problem below, extract only its structural parameters — do not solve it.

Problem:
{problem_text}

Extract the following:
- investors: the exact names of all investors mentioned in the transaction section
- question_type: "sell" (the investors need to sell their remaining shares to achieve a profit target) or "buy" (they need to buy shares to achieve a target)
- target_percentage: the target profit percentage as a float (e.g. 1.3 for "1.3% profit")
- aggregation: "earliest" (the question asks who achieves the target FIRST / earliest date) or "latest" (who achieves it LAST / latest date among the investors' first valid dates)
- price_type: "Open" or "Close" (which daily price is used — look for "opening prices" or "closing prices" in the question)

Return only the structured fields. Do not perform any calculations.
